// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://santisanchez4.github.io',
  base: '/portfolio',
  output: 'static',
});
